<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/" style="color: var(--primary-color);">
      <i class="fas fa-rocket me-2"></i>NASCON 2025 (Islamabad)
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= typeof title !== 'undefined' && title === 'Home' ? 'active' : '' %>" href="/"><i class="fas fa-home me-1"></i>Home</a>
        </li>
        <% if (locals.user && locals.user.privileges) { %>
          <% if (user.privileges.Events && user.privileges.Events.read) { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title === 'Events' ? 'active' : '' %>" href="/events"><i class="fas fa-calendar-alt me-1"></i>Events</a>
            </li>
          <% } %>
          <% if (user.privileges.Venues && user.privileges.Venues.read) { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title === 'Venues' ? 'active' : '' %>" href="/venues"><i class="fas fa-map-marker-alt me-1"></i>Venues</a>
            </li>
          <% } %>
           <% if (user.privileges.Sponsors && user.privileges.Sponsors.read) { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title === 'Sponsors' ? 'active' : '' %>" href="/sponsors"><i class="fas fa-handshake me-1"></i>Sponsors</a>
            </li>
           <% } %>
           <% if (user.privileges.Accommodations && user.privileges.Accommodations.read) { %>
             <li class="nav-item">
               <a class="nav-link <%= typeof title !== 'undefined' && title === 'Accommodations' ? 'active' : '' %>" href="/accommodations"><i class="fas fa-bed me-1"></i>Accommodations</a>
             </li>
           <% } %>
           <% if (user.privileges.Payments && user.privileges.Payments.read) { %>
             <li class="nav-item">
               <a class="nav-link <%= typeof title !== 'undefined' && title === 'Payments' ? 'active' : '' %>" href="/payments"><i class="fas fa-dollar-sign me-1"></i>Payments</a>
             </li>
           <% } %>
           <% if (user.privileges.Judges && user.privileges.Judges.read) { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title === 'Judges' ? 'active' : '' %>" href="/judges"><i class="fas fa-gavel me-1"></i>Judges</a>
            </li>
           <% } %>
          <% } %>
        <li class="nav-item">
          <a class="nav-link <%= typeof title !== 'undefined' && title === 'About' ? 'active' : '' %>" href="/about"><i class="fas fa-info-circle me-1"></i>About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= typeof title !== 'undefined' && title === 'Contact' ? 'active' : '' %>" href="/contact"><i class="fas fa-envelope me-1"></i>Contact</a>
        </li>
      </ul>

      <ul class="navbar-nav ms-auto">
        <% if (locals.user) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fas fa-user-circle me-1"></i><%= user.Name || user.username %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li><a class="dropdown-item" href="/profile"><i class="fas fa-user me-2"></i>Profile</a></li>
              <li><a class="dropdown-item" href="/dashboard"><i class="fas fa-tachometer-alt me-2"></i>Dashboard</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/auth/logout"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/login"><i class="fas fa-sign-in-alt me-1"></i>Login</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-primary ms-2" href="/register"><i class="fas fa-user-plus me-1"></i>Register</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
